<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AreaNotice">

	<!-- Area_Like userid, dongcode insert -->
	<insert id="insertAreaLike" parameterType="hashmap">
		insert into
		AREA_NOTICE
		(userid, dongcode)
		values
		(#{userid}, #{dongcode})
	</insert>

	<!-- board boardid, regdate insert -->
	<update id="insertBoard" parameterType="hashmap">
		update area_notice set
		boardid = #{boardid} , board_regdate= #{board_regdate} where dongcode=
		#{dongcode};
	</update>
    <!-- 읽었을 때 1로 update, 읽음처리-->
	<update id="updateReaded" parameterType="int">
		update area_notice set readed = 1 where boardid= #{boardid};
	</update>
	
	<select id="selectNotice" parameterType="hashmap" resultType="AreaNotice">
	   select * from area_notice where userid = #{userid} order by board_regdate desc
	</select>



</mapper>