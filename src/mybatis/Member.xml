<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">
	<insert id="insertmember" parameterType="hashmap">
		INSERT INTO
		MEMBER(userid, pwd, name,email,birthdate,gender,regdate,author)
		VALUES(#{userid}, #{pwd}, #{name}, #{email}, #{birthdate}, #{gender},#{regdate}, #{author})
	</insert>

	<select id="checkId" parameterType="hashmap" resultType="String">
		SELECT
		userid FROM Member WHERE userid = #{userid}
	</select>

	<select id="loginMember" parameterType="hashmap"
		resultType="String">
		SELECT userid FROM Member WHERE userid = #{userid} AND pwd
		= #{pwd}
	</select>

	<select id="userInfo" resultType="hashmap">
		SELECT userid, pwd, name,email,birthdate,gender
		FROM MEMBER
		WHERE userid = #{userid}
	</select>


	<update id="upadatemember" parameterType="hashmap">
		UPDATE MEMBER
		SET name = #{name}, email = #{email}, birthdate = #{birthdate}
		WHERE userid = #{userid}
	</update>

	<update id="updatepassword" parameterType="hashmap">
		UPDATE MEMBER
		SET pwd = #{pwd}
		WHERE userid = #{userid}
	</update>

	<!-- <select id="passwordChk" parameterType="hashmap">
		SELECT count(*) FROM Member WHERE userid = #{userid} AND pwd = #{pwd}
	</select> -->

	<delete id="deletemember" parameterType="hashmap">
		DELETE FROM member WHERE userid = #{userid} and pwd = #{pwd}
	</delete>
</mapper>